<UserControl x:Class="SimpleCSVEditorByWPF.Views.CsvFileReadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleCSVEditorByWPF.Views" 
             xmlns:viewmodels="clr-namespace:SimpleCSVEditorByWPF.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:CsvFileReadViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
            <TextBox x:Name="CsvFilePathTextBox" AcceptsReturn="True" Margin="5" TextWrapping="Wrap" Width="585"
                     Text= "{Binding CsvFilePath, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="ファイル選択" Width="80" Margin="5" Command="{Binding SelectFileCommand}"/>
            <Button Content="ファイル読込" Width="80" Margin="5" Click="LoadCevFileButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
